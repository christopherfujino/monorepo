(library
 (name ntc_lang))

(menhir
  (modules parser)
  (explain true)
  ; --strict means fail on conflicts, will NOT generate .conflicts file
  (flags
    --exn-carries-state
    --strict
    ))

(ocamllex lexer)
